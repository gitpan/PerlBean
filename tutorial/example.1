#!/usr/bin/perl
  use strict;
  use PerlBean;
  my $bean = PerlBean->new ({
    package => 'My::Package',
    short_description => 'my test package',
  });
  use IO::File;
  my $fh = IO::File->new ("> $0.pm");
  $bean->write ($fh);

